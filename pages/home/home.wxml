<!--pages/home/home.wxml-->
<!-- 小程序数据绑定 -->
<view>Hello {{ name }}</view>
<view>年龄 {{ age }}</view>
<view wx:for="{{students}}">{{item.name}}</view>
<view>计数 {{counter}}</view>
<button open-type="getUserInfo"  bindgetuserinfo="handleClick">+</button>
<button>-</button>